# ForNext_Report — セッション再開用ブリーフィング

最終更新: 2026-08-13
状態: **Phase 2(プロトタイプ)一巡・停止中**。発注者が実際に arXiv 論文を数本読み、予習として機能するかを確認中。その結果で Phase 3 の優先順位を決める。**それまで判定層のパラメータは凍結**。
文書の所在は `docs/reference.md` 参照。実装・コマンドは本書に書かない(コードを読めば分かる)。

## 0. プロジェクトの骨格(初見者向け1分要約)

英語論文を「翻訳せずに読む」ための事前単語学習アプリ(ブラウザ完結・GitHub Pages・読者は発注者1人に固定、分野= arXiv cs.RO+cs.LG)。中核は L3「危険語」= 見た目日常語だが分野で語義がズレる語(return=収益, policy=方策)の自動検出。**縦軸(一般頻度)で候補プール → 横軸(分野でのズレ)で分類**する二軸モデル。判定は静的アルゴリズム(LLM禁止)、説明文の生成のみLLM(ビルド時プリベイク)。動くもの: テキスト貼付→L3/L2予習リスト(語義・出現文つき)→単語帳(3値ステータス)→読了後チェック(○/×、主指標=正答率80%)。

## 1. 主要な決定と、それを導いた実測データ

- **ゲート基準 v2(再現率優先)**: precision@50 ≥60% を撤回し「検証セット再現率≥18/20 かつ sense_shift≥9/10(必須)+ precision@50 ≥35%(目標)」へ。**理由: 誤りの価値非対称性** — 話題語の混入は known 1タップで自己修復する(分野の話題語彙は有限で数本読めば飽和)が、L3の取りこぼしはユーザーが気づけず永久に残る。契機は初回採点 precision@50=20% と、その修正で再現率が 18/20→14/20 に落ちた事件(「適合率を再現率で買う」は方向が逆、という発注者裁定)。最終ゲート: 再現率19/20・sense_shift 10/10・precision 38%で通過、heldout 9/10。
- **コーパス構成**: C = R(cs.RO含む)60% / L(cs.LGのみ・音響除外)40%、2021年以降。理由: 初版で sound の共起が respiratory/music になり「読まない分野の語彙が支配する」と判明。B = arXiv 22カテゴリ(cs.RO/LG/ML隣接除外、**非ML系CS 36%**)。理由: CS 12.5% の初版では propose/existing の文体差が delta で相殺されなかった(B拡張で delta 4割縮小を実測。policy/agent の名指し回帰で無破壊確認)。
- **順位付けの指数 p=0.5**(表示順位= score×tf×idf^0.5): p=1.0 では中df帯(trajectory/task/training = 分野頻出だが全論文には出ない、1本目の予習で known にすべき最重要学習帯)が圏外に沈む実測により決定。Jaccard 5-10% という当初基準は根拠なしとして破棄。
- **⚑(話題語疑い)はデフォルト非表示**: ⚑の実体は「C頻出+rgRel弱」で、真の話題語(robot)と妥当な高頻度危険語(force, observation)を区別できない(採点で適合率30%、実地3本でも本命 force に誤爆)。表示していたら force を読み飛ばすリスクがあった。
- **語義説明は案A(ビルド時全語プリベイク)**: 7,437語を Haiku で生成($3.63、失敗2語=トークナイザ破片のみ)。実行時APIなし= Phase 0 の「(a)完全静的」の枠内を維持。抜き取り検査30語で「Haikuは工学一般の語義に引っ張られ、sense_shift型の危険な語義を外す」パターンを確認 → **モデルを上げても解けない読者依存の判断**として、外れ6語は senses.yaml 手書きで対処(tight/flat/collapse は cs.RO側生成を保持して多義化、demonstration/hard/anchor は上書き)。実地で conditioning の誤りも発見・手書き修正(全数検品は不可能、使いながら直す運用が前提)。
- **粒度の分離原則**(複数決定の共通構造): 表示はグループ/状態は表層形(demonstration既知でもdemonstrate未知がありうる)、df は沈めるだけ/known は消す(dfで消すと1本目に学ぶ機会を失う)、予習に出さない層(L1b)も保存はする(表示上の判断≠保存の判断)。

## 2. 実測が設計を書き換えた箇所(次に同じ失敗をしないための記録)

1. **JSD(A,C)単独 → delta = JSD(A,C)−JSD(A,B)**: 単独では however/paper 等の学術レジスタずれを拾った。Bで相殺 → ところが**Bにも同語義がある語(mass/tight: mathの質量・タイトな上界)は真の信号まで相殺**され sense_shift 3/10 に暴落 → sense-replace ルート(jsdAC+rgRel)を追加して回収。
2. **replaceGen(絶対版)→ rgRel(B相対版)**: 字幕→論文のジャンル差で全語0.93〜1.0に飽和し弁別力ゼロ(実測)。「A側隣人の生存率の B相対比」に置換(increase はBでもCでも隣人が残る=語義不変、return はBで残りCで消える=置換)。
3. **topic-suspect 足切り → topic-flagged(表示区別)**: ガードの役割を「排除」から「区別」に転換。suspect 足切りは support/head/model/agent という妥当語を永久喪失させていた。⚑付きで通す(混入は自己修復側)。
4. **判定と順位付けの分離**: コーパス統計だけでは「分野の頻出語彙」が毎回上位に並び、3本流すと同じリストになる(重複 Jaccard 17%)。文書内特徴度(tf-idf、dfはビルド時焼き込み)を**別レイヤー**として導入 → 重複1〜2%、actor/critic/chunk など「この論文を読むために今日知るべき語」が上位に。
5. **説明文の(a)完全静的が実地で破綻 → ビルド時プリベイクで(a)の枠内を維持**: 手キュレーションでは7,400語をカバー不能と実地判明。「実行時静的」は守ったままLLMをビルド時に限定する案Aで解決。
6. 小さいが再発しやすいもの: log-odds z は**A完全欠落語で分散爆発して過小**(end-to-end z=3.5 vs fine-tuning z=21 → A欠落L2ルート追加)/ 改行ハイフン(tar- get)/ C内でだけ略語の同綴り(elf/ELF, dagger/DAgger)/ ベンチマーク名断片が tf を水増し(LIBERO-Long++ の Long → 文中大文字トークンは tf に数えない)。

## 3. 過適合の履歴(警告)

検証セットは使うたびに死ぬ、を4回繰り返した: **凍結20語**(5イテレーションで適合済み=検証セット化。以後回帰テスト専用)→ **採点50語**(θrr スイープ・deltaMaxJargon 導出に使用=検証セット化)→ **新50語**(採点済み)→ **heldout2**(チューニング完全未使用のまま1回だけゲートに使用。再現率9/10 = 汎化の最良推定)。**どのパラメータが何にフィットしているかの全数リストは algorithm.md §6-3b(パラメータ由来一覧)にあり、変更のたび更新する運用**。次のイテレーションでは: 閾値を採点済みデータから導出しない・評価セットに触れる回数を数える・「性能が上がった」は未使用セットでしか主張しない。**次の評価セットは発注者が実読で詰まった語**(想像で作った20語より質が高い)。

## 4. 保留中の既知問題(すべて判定層=凍結中。Phase 3 で優先順位づけ)

1. **sense-replace ルートのノイズ**(barely / hurt / long): 文書内出現1回でもL3に出る判定層ノイズ。出現回数での足切りは原則違反のため不可。実読の結果を見て対処。
2. **学術一般語の L3 混入**(across / jointly / convert / label / approach / challenge 系): 第2の誤り群。自己修復側だが第3信号判断と連動。
3. **L1b の定義**(単語帳の論文英語層、862語中妥当は respectively 級のみ): 根拠3診断 = θb の z膨張(two が z=48、効果量1.57倍)/ cache の zipf ミスマッチ(wordfreq=Web混みが高頻度プールに送るが字幕Aでは12回)/ state の既知度混入。**閾値調整では直らず定義から見直す**(architecture §7.3)。UIに精度注記済み。
4. **Gaussian 等の欠落**: 人名由来専門語(ほぼ常に大文字)が大文字率フィルタで固有名詞扱い。対処方針指示済み: -ian/-ean 免除が第一候補(ただし Fourier/Kalman/Lyapunov は語尾変化せず**複合語設計と不可分**)、優先は cs.LG 統計語彙(Gaussian, Bayesian, Markovian, Bernoulli, Dirichlet, Wasserstein)、ピンは受け皿(architecture §7.3)。

**独立した共通根本原因: 「rgRel / keyness / 共起分布が語単位で計算されていることによる、多義語の信号消滅」**。症状: mass(物理質量が支配し確率質量の信号が消滅→ピンで対処中)/ prior(prior work の形容詞用法が rgRel を壊す→⚑誤爆の主因)/ temperature(潜在)/ **R-L束ね検証 49.9%(基準ぎりぎり=「次で落ちる」)** / sound の構成依存。対処は承認済みの語義単位スキーマの延長 + R/L別統計(architecture §7.1)。**Phase 3 でこの根を叩くときにまとめて解決するか判断する。**

## 5. 未着手のもの

- **第3信号(凍結中)**: 投資判断は実読結果の後。**方向性の指定あり(phase2a_review4.md 必読)**: SGNS+Orthogonal Procrustes を第一候補にしない(C≈13Mトークンでは埋め込み学習に小さく、測りたい低頻度語で信号が壊れる)。**PPMI+SVD を共通語彙空間で構成すればアライメント自体が不要**になる案と比較して提案すること。それまで調査も禁止。
- **UIからの語義修正導線**(architecture §7.2 に設計記録済み): 生成語義の誤りは使いながら直す運用が前提で、「その場で追記できる」導線の有無が実用性を左右する。localStorage 即時反映+senses.yaml 形式エクスポートの二段構え案。
- **L4(略語: FOC, PMSM, MPC)**: Phase 3。文書内定義の抽出。大文字率フィルタで除外済みの略語群(7,900語規模)が原料になる。
- PDF入力(pdf.js)/ URL取り込み(最後)/ GitHub Pages への実デプロイ(Actions未設定)。

## 6. 次のステップ(合意済み)

1. **発注者が arXiv を数本、実際にこのツールで予習して読む**。詰まった語をメモ → それが次の評価セット(実読由来なので既存の正解セットより質が高い)
2. その結果で Phase 3 の優先順位を決める(候補: 上記4+1の既知問題、第3信号、L4、修正導線)
3. それまで判定層パラメータは凍結維持。UI側の不具合修正は随時可
4. 読了後チェックの正答率(主指標80%)と「丸投げせず読み切れたか」(副指標3本連続)がプロダクト評価の本丸
